import { SilmelikDirective } from './silmelik.directive';

describe('SilmelikDirective', () => {
  it('should create an instance', () => {
    const directive = new SilmelikDirective();
    expect(directive).toBeTruthy();
  });
});
