import { DynamicloadComponentDirective } from './dynamicload-component.directive';

describe('DynamicloadComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicloadComponentDirective();
    expect(directive).toBeTruthy();
  });
});
