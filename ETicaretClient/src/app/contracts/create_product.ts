export class Create_product {
    name: string;         // Figür adı
    series: string;       // Seri/evren
    category: string;     // Tür (Action Figure, Funko Pop, vb.)
    price: number;        // Fiyat
    stock: number;        // Stok adedi
    height: number;       // Boyut (cm)
    color: string;        // Renk
    description: string;  // Açıklama
    imageUrl?: string;
}
